#include<stdio.h>

void main(){
    int i=3,j;
    j=++i;
    printf("%d %d",i,j);
}
/*  Pre-increment (++var)
    1.increase the value of the variable-(i=i+1)
    2.use the updated value-(j=i)                 */